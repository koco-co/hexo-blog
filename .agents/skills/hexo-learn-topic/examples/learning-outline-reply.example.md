# 计算机网络基础学习大纲

> 这是什么：这是你接下来学习计算机网络基础所需的主题和 Hexo 文章安排，不是已经写好的课程正文。
>
> 你现在重点看：要学的主题是否完整，每个学习主题是否准确对应一篇同名文章，以及图解和闪卡是否覆盖 HTTP、TCP 和三次握手等重点。
>
> 你现在只需做：确认或调整这份学习大纲。确认前不会创建任何文件。

## 你学完能够做什么

- 解释浏览器输入网址后，DNS、IP、TCP/QUIC、TLS 和 HTTP 如何协作。
- 看懂 IP 地址、子网掩码、端口、路由表和常见网络报文。
- 不靠背步骤，解释 TCP 三次握手、四次挥手、重传和拥塞控制。
- 区分 HTTP/1.1、HTTP/2、HTTP/3 及 HTTPS。
- 使用 `ping`、`dig`、`curl`、`traceroute`、`tcpdump` 和浏览器开发者工具排查网络问题。
- 回答常见计算机网络面试题，并能应对继续追问。

## 需要学习的主题

| 学习主题 | 需要掌握什么 | 学完后的结果 |
| --- | --- | --- |
| 网络分层与局域网 | OSI、TCP/IP、数据封装、MAC、交换机、ARP 和 IPv6 邻居发现 | 能解释同一局域网中的数据如何发送 |
| IP 与网络寻址 | IPv4、IPv6、CIDR、子网、ICMP、路由、NAT、DHCP 和 DNS | 能计算子网并说明数据如何找到目标 |
| UDP 与 TCP | 端口、Socket、TCP 报文、连接、重传、窗口和拥塞控制 | 能解释三次握手、四次挥手和可靠传输 |
| HTTP 与 HTTPS | HTTP 语义、缓存、Cookie、HTTP/1.1、HTTP/2、HTTP/3、QUIC 和 TLS | 能分析现代浏览器请求及协议版本差异 |
| 抓包与排障 | `ping`、`dig`、`curl`、`traceroute`、`tcpdump`、Chrome Network 和 Wireshark | 能从现象逐层定位网络问题 |

文章数量：`5` 个学习主题 + `1` 篇入门路线 + `1` 篇进阶路线 + `1` 篇综合实战 = `8` 篇。

## Hexo 文章安排

| 序号 | 文章标题 | 这篇学习什么 | 图解或实验 | 闪卡重点 |
| ---: | --- | --- | --- | --- |
| 一 | 计算机网络(一)入门路线 | 课程目标、阅读顺序和文章依赖 | 课程路线图 | 无 |
| 二 | 计算机网络(二)网络分层与局域网 | OSI、TCP/IP、封装、MAC、交换机、ARP 和邻居发现 | 分层数据旅程图、ARP 时序图与抓包 | OSI 与 TCP/IP、交换机与路由器、ARP |
| 三 | 计算机网络(三)IP 与网络寻址 | IPv4、IPv6、CIDR、子网、ICMP、路由、NAT、DHCP 和 DNS | 子网计算、路由转发、DHCP 与 DNS 时序图 | 子网、路由、NAT、DNS 使用 TCP 还是 UDP |
| 四 | 计算机网络(四)UDP 与 TCP | 端口、Socket、报文、连接、可靠传输、窗口和拥塞控制 | 握手挥手时序图、状态机、窗口曲线与抓包 | 三次握手、四次挥手、重传、TIME_WAIT、拥塞控制 |
| 五 | 计算机网络(五)HTTP 与 HTTPS | HTTP 语义、缓存、Cookie、协议版本、QUIC、证书和 TLS | HTTP 报文、缓存流程、协议栈对比和 TLS 握手图 | 幂等性、状态码、304、HTTP/2、HTTP/3、HTTPS |
| 六 | 计算机网络(六)抓包与排障 | 命令行、Chrome Network、tcpdump 和 Wireshark | 分层排障流程与标注抓包截图 | ping 不通但 HTTP 可用、502/504、页面慢 |
| 七 | 计算机网络(七)进阶路线 | 低频协议能力、专项网络环境和继续深入的方向 | 专项能力索引与选择图 | 何时继续学习专项主题 |
| 八 | 计算机网络(八)综合实战 | 从输入 URL 到响应，完成全链路分析和故障定位 | 全链路图和综合实验 | 综合场景追问 |

## 基于你的旧笔记补充

保留旧笔记中的分层、CIDR、TCP、HTTP、TLS 和抓包实践；补充 IPv6、DHCP、DNS、路由、NAT、TCP 可靠性以及 HTTP/2、HTTP/3；过时或不准确的协议图重新绘制，不直接搬运。

## 范围边界

- 本课程不展开企业级路由配置、内核协议栈源码和攻击利用。

## 你现在只需要确认

请确认：

**这些学习主题和 Hexo 文章安排是否符合你的目标？**

确认后我会设计每篇文章的详细章节、图解、实验和闪卡；仍不会立即创建文章。
