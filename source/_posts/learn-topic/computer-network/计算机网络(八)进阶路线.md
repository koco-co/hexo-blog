---
title: "计算机网络(八)进阶路线"
tags:
  - "计算机网络"
  - "WebSocket"
  - "网络安全"
categories:
  - "Learn Topic"
  - "计算机网络"
description: "在主线完成后，用 WebSocket、DoH/DoT、IPsec、VXLAN 扩展网络视野，并明确每个主题的层次、取舍和进入条件。"
cover: /img/picgo-images/computer-network-course-cover.png
series: "计算机网络"
series_order: 8
published: false
learn_topic_capabilities:
  - "ADV-001"
  - "ADV-002"
  - "ADV-003"
  - "ADV-004"
abbrlink: e470b049
date: 2026-08-25 08:00:00
---

<!-- learn-topic-placeholder -->

{% course_series %}

> 本文仍保留课程占位标记，表示它正在按公开候选门禁验收；下面的内容已经是正式正文草稿。

## 本文职责

主线课程先保证 HTTP、TCP、TLS、IP 和排障能够独立解释；本文再回答“下一步为什么学 WebSocket、DoH/DoT、IPsec、VXLAN，以及它们会改变哪一层”。这些主题是可选扩展，不应反过来打断三次握手和 HTTP 基础。

完成本文后，你应该能够：

- 说明 WebSocket 从 HTTP Upgrade 进入长连接帧的边界；
- 对比 DoH 与 DoT 的加密位置、可观测性、部署和集中化取舍；
- 把 IPsec 的安全关联、ESP/AH、传输/隧道模式放回 IP 层；
- 解释 VXLAN 的 VNI、VTEP、UDP 封装和 underlay/overlay 关系；
- 根据问题和现有证据选择进阶主题，而不是为了记名词继续扩展。

前置文章是[计算机网络(一)入门路线](/posts/347ce4f2/)、[计算机网络(五)HTTP 协议](/posts/366484e7/)、[计算机网络(六)HTTPS 与现代 HTTP](/posts/36cb963b/)和[计算机网络(七)抓包与网络排障](/posts/61ba71fc/)。本文分配能力为 ADV-001～ADV-004。

## 正文大纲

- H2：扩展协议地图；
- H2：应用与名称：WebSocket、DoH、DoT；
- H2：安全与覆盖：IPsec、VXLAN；
- H2：进阶门禁与复习路径；
- H2：常见问题；
- H2：参考资料。

## 扩展协议地图

### 先定位“改变了哪一层”

{% mermaid %}
flowchart TD
  MAIN[主线：DNS/IP/TCP/TLS/HTTP/排障]
  MAIN --> WS[WebSocket：应用层升级与消息帧]
  MAIN --> DOH[DoH/DoT：DNS 查询的安全承载]
  MAIN --> IPSEC[IPsec：IP 层安全架构]
  MAIN --> VXLAN[VXLAN：数据中心二层覆盖到三层 underlay]
  WS -->|依赖 HTTP Upgrade、TLS、浏览器 Origin| HTTPBASE[HTTP/TLS 基础]
  DOH -->|依赖 DNS、TLS/HTTP、证书| NAME[名称解析基础]
  IPSEC -->|依赖 IP、路由、密钥协商| IPBASE[IP 基础]
  VXLAN -->|依赖以太网、UDP、路由和 MTU| FABRIC[局域网与 IP 基础]
{% endmermaid %}

| 主题 | 它扩展的对象 | 先学什么 | 不要把它误认为 |
| --- | --- | --- | --- |
| WebSocket | HTTP 之后的双向应用消息 | HTTP Upgrade、TLS、浏览器 Origin | “TCP 自动变成消息队列” |
| DoH | DNS 查询通过 HTTPS 发送 | DNS 递归、HTTPS、HTTP/2/3 | “所有 DNS 都被隐藏” |
| DoT | DNS 查询通过 TLS 长连接发送 | DNS、TLS、端口 853 语境 | “比 DoH 在所有网络都更快” |
| IPsec | IP 包的认证/加密和隧道 | 路由、策略、密钥管理 | “TLS 的另一个名字” |
| VXLAN | 二层帧跨三层网络的覆盖封装 | VLAN、UDP、MTU、VTEP | “把任何二层网络无限延伸” |

## 应用与名称：WebSocket、DoH、DoT

### WebSocket 的升级边界

WebSocket 通常从 HTTP/1.1 Upgrade 请求开始，服务端返回 101 Switching Protocols 后，双方不再把后续字节当作普通 HTTP 请求/响应，而是进入 WebSocket 帧语义。它仍然依赖底层 TCP（或安全的 wss 连接依赖 TLS），但应用层消息边界由 WebSocket 帧定义。

{% mermaid %}
sequenceDiagram
  participant B as 浏览器
  participant S as WebSocket 服务
  B->>S: GET /chat + Upgrade: websocket + Sec-WebSocket-Key
  S-->>B: 101 Switching Protocols + Sec-WebSocket-Accept
  B->>S: WebSocket text/binary frame
  S-->>B: WebSocket frame
  Note over B,S: 后续不再按普通 HTTP 请求/响应配对
{% endmermaid %}

关键边界：

- 101 是升级握手的 HTTP 结果，不是普通业务成功码；
- WebSocket 有自己的帧、ping/pong、close 和掩码规则；
- 浏览器会携带 Origin，服务端需要在应用层决定是否允许来源；
- 代理、负载均衡、空闲超时和连接复用策略都可能影响长连接；
- WebSocket 不自动提供消息持久化、重试、顺序业务语义或鉴权撤销。

复现时先用服务端和客户端都明确支持 WebSocket 的测试夹具；只用 curl 发一个 Upgrade 头，不代表已经完成了后续 WebSocket 帧通信。观察 101、连接保持、ping/pong 和 close 时序，比只看页面“连接成功”更有证据价值。

### DoH 与 DoT 的取舍

DoH 把 DNS 查询放进 HTTPS，通常复用 HTTP/2 或 HTTP/3 的连接和端口；DoT 把 DNS 查询放进 TLS，通常使用专用的 TLS 服务端口。二者都能保护客户端到指定解析器之间的查询内容，但不能自动隐藏访问目标、终端行为、解析器日志或后续连接。

| 维度 | DoH | DoT |
| --- | --- | --- |
| 承载 | HTTPS | TLS 上的 DNS |
| 常见入口 | 与 Web 流量相近的 HTTPS 端口 | 专用 DNS over TLS 端口 |
| 中间网络可见性 | 更像普通 HTTPS，策略识别更难 | 更容易按专用端口识别/治理 |
| 部署形态 | 浏览器、操作系统或应用指定 HTTPS resolver | 系统 resolver 或网络设备指定 TLS resolver |
| 取舍 | 易穿过只允许 Web 的网络，但可能集中化 | 语义直接、网络治理清晰，但专用端口可能被阻断 |

选择时要问：谁运营解析器、失败时是否回退明文、如何验证证书、是否需要企业审计和家长控制、客户端是否会绕过本地策略。不要把“加密了 DNS”扩展成“端到端所有流量都匿名”。

### 名称解析和连接证据

DoH/DoT 解决的是 DNS 查询承载，不能替代后续目标服务的 TCP/QUIC、TLS 和 HTTP 证据。排障时可以把链路拆成：应用选择 resolver → 解析器返回地址 → 客户端连接地址 → 目标服务建立安全协议。若只看到 DoH 成功，不代表目标 IP 可达。

## 安全与覆盖：IPsec、VXLAN

### IPsec 的安全模型

IPsec 在 IP 层提供安全架构，核心是按策略决定哪些流量需要保护，并通过安全关联和密钥管理确定如何认证/加密。ESP 可以提供机密性、完整性和认证等能力；AH 关注认证/完整性但在 NAT 等环境中有现实边界。传输模式通常保护 IP 载荷，隧道模式把整个原始 IP 包封装进新的 IP 包。

{% mermaid %}
flowchart LR
  POLICY[安全策略选择流量] --> SA[Security Association 与密钥]
  SA --> ESP[ESP/AH 处理]
  ESP --> TRANS[传输模式：保护载荷]
  ESP --> TUNNEL[隧道模式：封装原始 IP 包]
  TUNNEL --> PEER[对端解封装并按策略交付]
{% endmermaid %}

IPsec 与 TLS 的边界不同：TLS 通常由应用连接端点或代理终止，IPsec 可以保护一段网络到网络、主机到网络的 IP 路径。两者可以叠加，也可能由不同团队、设备和信任域管理。看到“隧道建立”不能直接推断应用授权和端点身份已经完成。

### VXLAN 的 underlay/overlay

VXLAN 用 VNI 标识覆盖网络，把原始二层帧封装在 UDP/IP 外层，常见 VTEP 负责封装和解封装。底层 underlay 只需要为 VTEP 之间提供三层可达性，overlay 则让虚拟机或租户看到逻辑二层网络。

{% mermaid %}
flowchart LR
  VM1[租户主机 A] --> VTEP1[VTEP 1]
  VTEP1 -->|外层 IP/UDP + VNI| UNDERLAY[三层 Underlay]
  UNDERLAY --> VTEP2[VTEP 2]
  VTEP2 --> VM2[租户主机 B]
  VTEP1 -.Overlay 广播/组播或控制面学习.-> VTEP2
{% endmermaid %}

VXLAN 的排障重点包括 VTEP 外层 IP 可达性、UDP 端口、VNI、封装后的 MTU、控制面/数据面学习和跨租户隔离。它不是让二层广播消失的魔法；广播、未知单播和组播在 overlay 中仍需要复制、控制面或硬件策略，规模化时必须关注泛洪范围。

## 进阶门禁与复习路径

### 什么时候进入下一个主题

| 如果你正在解决 | 先回到 | 只有出现这些证据时再进入进阶 |
| --- | --- | --- |
| 页面实时推送断开 | TCP/TLS/HTTP 与连接超时 | 101 成功但帧、ping/pong、代理空闲策略仍需分析 |
| DNS 被拦截或隐私要求 | DNS、证书和 resolver 策略 | 需要选择 DoH/DoT 的治理和承载方式 |
| 跨网络安全互联 | IP、路由、密钥和策略 | 需要网络层隧道或网段到网段保护 |
| 数据中心租户网络 | VLAN、MTU、路由和 underlay | 需要覆盖二层、VNI、VTEP 和大规模隔离 |

### 复习卡片：只复用前置问题

下列卡片不是本篇新建的重复卡，而是把进阶主题挂到前置的稳定复习节点：

{% flashcard_ref id="CN-HTTP-004" %}
{% flashcard_ref id="CN-HTTP-009" %}
{% flashcard_ref id="CN-MOD-005" %}
{% flashcard_ref id="CN-MOD-007" %}
{% flashcard_ref id="CN-DIAG-004" %}

建议复习顺序是：先回答方法/协议边界，再回答 TLS 与证书，最后把扩展协议放回 DNS、IP 和排障证据链。若无法解释 HTTP Upgrade、证书主机名或 UDP/QUIC 的连接状态，就暂缓进入 WebSocket、DoH/DoT 或 VXLAN 的实现细节。

## 常见问题

### WebSocket 是不是“更快的 HTTP”？

不是。它通常先通过 HTTP Upgrade 建立入口，随后使用不同的帧和长连接语义。性能和体验还取决于代理、心跳、消息大小、重连和服务端资源，不能只按协议名称比较快慢。

### DoH 比 DoT 更安全吗？

不能脱离威胁模型比较。二者都可以保护客户端到解析器的查询传输，但解析器信任、日志、端点策略、网络治理和回退行为决定了实际风险。DoH 更像 HTTPS，DoT 的专用端口更容易被识别和治理。

### IPsec 和 HTTPS 需要同时使用吗？

它们保护的边界不同，是否叠加取决于网络拓扑和威胁模型。IPsec 可以保护网络路径，HTTPS 保护应用连接；IPsec 隧道建立也不替代 HTTP 认证与授权。

### VXLAN 能解决所有 VLAN 扩展问题吗？

不能。它增加了覆盖封装和规模化隔离能力，但引入 VTEP、MTU、控制面、泛洪和 underlay 可达性问题。是否采用应由租户数量、故障域、控制面和运维能力决定。

## 参考资料

{% linkgroup %}
{% link RFC 6455：The WebSocket Protocol, https://www.rfc-editor.org/rfc/rfc6455.html, https://www.rfc-editor.org/rfc/rfc6455.txt %}
{% link RFC 8484：DNS Queries over HTTPS, https://www.rfc-editor.org/rfc/rfc8484.html, https://www.rfc-editor.org/rfc/rfc8484.txt %}
{% link RFC 7858：Specification for DNS over TLS, https://www.rfc-editor.org/rfc/rfc7858.html, https://www.rfc-editor.org/rfc/rfc7858.txt %}
{% link RFC 4301：Security Architecture for IP, https://www.rfc-editor.org/rfc/rfc4301.html, https://www.rfc-editor.org/rfc/rfc4301.txt %}
{% link RFC 7348：Virtual eXtensible Local Area Network, https://www.rfc-editor.org/rfc/rfc7348.html, https://www.rfc-editor.org/rfc/rfc7348.txt %}
{% endlinkgroup %}
