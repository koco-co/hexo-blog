---
title: AI 大模型应用(四)Prompt与任务契约
tags:
  - 大模型应用
categories:
  - Learn Topic
  - AI 大模型应用
description: 为六类合成工单建立输入、约束、输出、拒答和验收字段，并能识别冲突指令。
cover: /img/picgo-images/llm-application-course-cover.png
series: AI 大模型应用
series_order: 4
published: false
abbrlink: 4d0efd05
date: 2026-08-29 00:00:00
---

<!-- learn-topic-placeholder -->

{% course_series %}

> 本文是已确认课程中的未发布占位文章；下面只记录写作边界，不代表正文已经完成。

## 文章职责

- 唯一要解决的问题：把自然语言目标改写成可测量、可版本化、可审查的任务契约。
- 可观察成果：为六类合成工单建立输入、约束、输出、拒答和验收字段，并能识别冲突指令。
- 明确不承担：不重复前置文章的通用定义；跨主题扩展交给对应后续文章。

## 内容边界

- 进入条件：完成 A03 的相关实验与边界判断。
- 覆盖条目：任务拆解、契约字段、版本变更、注入边界、实验复盘
- 失败边界：对缺少版本、冲突要求、伪造命令和边界输入的工单执行契约检查。 还必须说明不适用条件、降级路径和不能由本篇单独证明的结论。

## 正文编排

| H2/H3 与正文块 | 读者任务 | 核心内容 | 主承载 | 选择理由 | 直接可见 | 失败降级 | 证据或示例 | 验证状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 任务拆解 | 把自然语言目标改写成可测量、可版本化、可审查的任务契约。 | 任务拆解：为六类合成工单建立输入、约束、输出、拒答和验收字段，并能识别冲突指令。 | mermaid | 用用契约字段把“想要一个答案”拆成目标、证据、约束和完成条件。承载主心智模型，再以文字解释因果与边界。 | 核心判断、操作步骤、输入输出、风险和验收条件；对缺少版本、冲突要求、伪造命令和边界输入的工单执行契约检查。 | 标签、图表或外部资源失效时，保留表格、列表、命令和文字结论。 | 对缺少版本、冲突要求、伪造命令和边界输入的工单执行契约检查。 | planned |
| 契约字段 | 完成“契约字段”中的关键理解、操作或判断 | 契约字段：为六类合成工单建立输入、约束、输出、拒答和验收字段，并能识别冲突指令。 | table | 该块只承担“契约字段”这一任务，避免把概念、操作和验收混成一段。 | 核心判断、操作步骤、输入输出、风险和验收条件；对缺少版本、冲突要求、伪造命令和边界输入的工单执行契约检查。 | 标签、图表或外部资源失效时，保留表格、列表、命令和文字结论。 | 对缺少版本、冲突要求、伪造命令和边界输入的工单执行契约检查。 | planned |
| 版本变更 | 完成“版本变更”中的关键理解、操作或判断 | 版本变更：为六类合成工单建立输入、约束、输出、拒答和验收字段，并能识别冲突指令。 | list | 该块只承担“版本变更”这一任务，避免把概念、操作和验收混成一段。 | 核心判断、操作步骤、输入输出、风险和验收条件；对缺少版本、冲突要求、伪造命令和边界输入的工单执行契约检查。 | 标签、图表或外部资源失效时，保留表格、列表、命令和文字结论。 | 对缺少版本、冲突要求、伪造命令和边界输入的工单执行契约检查。 | planned |
| 注入边界 | 完成“注入边界”中的关键理解、操作或判断 | 注入边界：为六类合成工单建立输入、约束、输出、拒答和验收字段，并能识别冲突指令。 | table | 该块只承担“注入边界”这一任务，避免把概念、操作和验收混成一段。 | 核心判断、操作步骤、输入输出、风险和验收条件；对缺少版本、冲突要求、伪造命令和边界输入的工单执行契约检查。 | 标签、图表或外部资源失效时，保留表格、列表、命令和文字结论。 | 对缺少版本、冲突要求、伪造命令和边界输入的工单执行契约检查。 | planned |
| 实验复盘 | 完成“实验复盘”中的关键理解、操作或判断 | 实验复盘：为六类合成工单建立输入、约束、输出、拒答和验收字段，并能识别冲突指令。 | flashcard | 该块只承担“实验复盘”这一任务，避免把概念、操作和验收混成一段。 | 核心判断、操作步骤、输入输出、风险和验收条件；对缺少版本、冲突要求、伪造命令和边界输入的工单执行契约检查。 | 标签、图表或外部资源失效时，保留表格、列表、命令和文字结论。 | 对缺少版本、冲突要求、伪造命令和边界输入的工单执行契约检查。 | planned |

## 视觉与复习

- 难点理解计划：以用契约字段把“想要一个答案”拆成目标、证据、约束和完成条件。作为主心智模型，先给出观察或反例，再解释真实机制、隐喻边界和迁移检查。
- 标签选型复查：写作时从当前完整标签目录选择；禁止 tip，note 只按语义使用 flat，长原理用 folding，平行实现才使用 tabs。
- 图表或实验：用契约字段把“想要一个答案”拆成目标、证据、约束和完成条件。；对缺少版本、冲突要求、伪造命令和边界输入的工单执行契约检查。
- 复习卡片：flashcard_ref: a04-prompt-contract；flashcard_ref: a04-xml-is-not-defense
- 参考资料卡片：OpenAI Platform documentation（https://platform.openai.com/docs/overview）；Anthropic API documentation（https://docs.anthropic.com/en/docs/intro）

## 验收证据

- 机械检查：运行 content、tags、assets、lint 与闪卡相关检查，修复具体文件错误。
- 隔离构建：在隔离草稿环境完成构建、桌面与移动路由检查，确认标签、图片、降级和课程导航。
- 正文完成条件：补齐真实机制、可复现实验、失败边界、参考资料和必要闪卡；独立复核通过后删除占位标记并切换为 published: true。
