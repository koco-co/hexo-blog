---
title: 测试基础(八)规则组合与判定测试
tags:
  - 测试基础
  - 系统学习
categories:
  - Learn Topic
  - 测试基础
description: 把多条件业务规则转换成可验证组合，产出可追踪判定表和一组有依据的功能用例。
cover: /img/learn-topic/testing-basics/course-cover.png
series: 测试基础
series_order: 8
published: true
abbrlink: ed62e469
date: 2026-05-21 00:00:00
---

{% course_series %}

{% note info flat %}
当多个条件共同决定结果时，单独测试每个输入不够。先把条件、动作、互斥和依赖写清，再用因果图和判定表保留有意义的组合；组合爆炸时按风险与业务约束缩减，而不是随意删列。
{% endnote %}

## 规则建模

{% note primary flat %}
以借阅资格为例，条件可以是“会员有效”“未超过上限”“图书可借”，动作是“允许借阅”或“拒绝并说明原因”。每个条件要有定义和取值，优先级要明确，否则判定表只是格式化的猜测。
{% endnote %}

| 条件 | Y | N | 依赖/备注 |
| --- | --- | --- | --- |
| 会员有效 | 可继续判断 | 拒绝 | 访客不可借阅（教学规则） |
| 借阅数 < 3 | 可继续判断 | 拒绝 | 上限按册数还是种类需确认 |
| 图书库存 > 0 | 允许借阅 | 拒绝 | 库存未知不能当作 0 |

![黑盒规则从原因、约束到结果的关系](/img/learn-topic/testing-basics/borrowing-causes-effects.jpeg "借阅原因与结果")

## 逻辑关系

{% mermaid %}
flowchart LR
  A[会员有效] --> D{所有必要条件满足?}
  B[未超过上限] --> D
  C[库存可借] --> D
  D -->|是| E[创建借阅记录并扣减库存]
  D -->|否| F[拒绝并给出可行动原因]
{% endmermaid %}

![因果图常用的与、或、非和约束符号](/img/learn-topic/testing-basics/cause-effect-symbols.jpeg "因果图符号")

{% note info flat %}
因果图适合把自然语言中的条件关系显式化，判定表则把可行组合变成测试行。互斥、强制、屏蔽和优先级必须写出来；“所有条件都能自由取 Y/N”通常会生成并不存在的业务状态。
{% endnote %}

## 构造判定表

| 规则列 | 会员有效 | 未超上限 | 库存可借 | 动作 | 用例 |
| --- | :---: | :---: | :---: | --- | --- |
| R1 | Y | Y | Y | 允许并记录借阅 | C01 |
| R2 | N | - | - | 拒绝：账号无资格 | C02 |
| R3 | Y | N | - | 拒绝：达到上限 | C03 |
| R4 | Y | Y | N | 拒绝：无库存 | C04 |
| R5 | Y | N | N | 按优先级返回上限原因，并记录未检查库存 | C05 |

{% note warning flat %}
横杠 `-` 表示该条件对当前动作不产生区别，不是“随便填值”。如果不同取值会导致不同错误提示、审计记录或副作用，就不能合并为横杠。不可行组合也应单独标记并说明为何不生成用例。
{% endnote %}

## 简化与推导

| 简化动作 | 可以合并的前提 | 不能合并的信号 |
| --- | --- | --- |
| 合并规则列 | 动作、错误码、状态和日志完全相同 | 文案、优先级或副作用不同 |
| 用代表值替代 | 同一等价类且无交互 | 边界或跨字段约束不同 |
| 标记不可行 | 需求明确禁止组合 | 只是测试者没有准备数据 |
| 保留冗余检查 | 能发现不同回归风险 | 只为了增加用例数量 |

{% note success flat %}
每一列都应能追溯到一个规则、一个或多个用例和具体预期。简化的目标是减少无价值重复，不是让表格看起来更小；删掉一列前先说明风险和替代证据。
{% endnote %}

## 组合练习

| 场景 | 条件 | 代表组合 | 关键预期 |
| --- | --- | --- | --- |
| 优惠与运费 | 会员、满额、免邮地区 | 全满足、仅一项满足、互斥组合 | 折扣与运费各自可解释 |
| 资格与分流 | 登录、实名、年龄段 | 未登录、已登录未实名、已实名 | 进入正确流程且不泄露受限信息 |
| 文件处理 | 类型、大小、权限 | 合法、类型错、超限、无权限 | 错误归因、临时文件清理 |

## 方法边界

{% note warning flat %}
条件从 3 个增加到 10 个时，全组合会迅速膨胀。可以按风险选择代表组合、保留约束并对关键规则做独立验证；若交互强度成为主要风险，再进入组合测试等进阶方法。判定表不能替代状态、时序和性能模型。
{% endnote %}

## 常见问题

{% flashcard basic id:testing-basics-scenario-discount-shipping deck:"测试基础" priority:1 tags:"场景,优惠,判定表" %}
--- question
给定优惠与运费规则，如何设计组合测试？
--- answer
列出会员、满额、地区等条件，先标互斥和优先级，再用判定表覆盖全满足、单条件和不可行组合。
--- explanation
先确认优惠是否可叠加、满额按折前还是折后金额、地区是否免邮。代表列应包括：会员且满额且免邮；只有会员；只有满额；不满足任何条件；互斥优惠同时出现。预期写折扣金额、运费、最终金额和订单记录，不能只看页面标签。若不可行组合由规则明确禁止，标记为约束而非强行制造数据。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-qualification-routing deck:"测试基础" priority:1 tags:"场景,资格,分流" %}
--- question
资格判断和流程分流如何用判定表覆盖？
--- answer
把登录、实名、年龄或权限作为条件，覆盖每个分流出口及条件冲突时的优先级和信息隐藏。
--- explanation
教学规则要先写清：未登录进入登录、已登录未实名进入认证、已认证进入业务；若年龄或组织权限再参与判断，就增加对应条件。每条规则明确页面/接口出口、返回状态和是否创建临时数据。冲突时应验证最安全或最明确的优先级，不让前端显示受限资源名称。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-file-rules deck:"测试基础" priority:1 tags:"场景,文件,规则组合" %}
--- question
文件类型、大小和权限规则如何组合？
--- answer
先单独覆盖每项有效/无效分区，再用少量合法组合验证类型、大小、权限共同决定的动作和清理结果。
--- explanation
代表组合包括允许类型且大小合规且有权限（成功）、类型不符、大小超限、无权限，以及类型和大小都错但错误优先级已定义的情况。预期同时检查错误原因、服务器是否保存部分文件、用户是否可见和重试是否幂等。避免把所有非法条件塞成一条而失去归因。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-refund-rules deck:"测试基础" priority:1 tags:"场景,退款,判定表" %}
--- question
退款资格、时间和金额规则如何设计组合？
--- answer
把订单状态、申请时间、退款类型和已退款金额列成条件，覆盖可退、超时、部分退款和重复申请的状态结果。
--- explanation
先固定教学假设：已支付且未完成售后的订单可申请，超出约定时间拒绝，部分退款不能超过可退余额。代表用例要检查订单、退款单和账户余额的一致性；网络超时或重复点击时，使用幂等键或查询结果确认是否已经处理。法律时限、真实支付和平台规则不能凭经验代入练习。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-inventory-rule deck:"测试基础" priority:1 tags:"场景,库存,规则" %}
--- question
库存、购买量和会员资格的组合如何验证？
--- answer
将库存是否充足、购买量边界和会员资格作为条件，覆盖成功、超量、无货、无资格及竞争更新后的最终状态。
--- explanation
如果库存为 5、购买量允许 1～5，先测 1、5、6 和库存为 0；再组合会员有效/无效与库存变化。预期写订单数量、剩余库存、拒绝原因和重复提交的幂等结果。两名用户同时购买最后一件属于并发与状态边界，需用可控夹具验证只有一个成功且库存不为负。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-notification-rules deck:"测试基础" priority:1 tags:"场景,通知,判定表" %}
--- question
通知渠道、用户偏好和失败重试如何设计组合？
--- answer
把渠道可用性、用户偏好、消息类型和重试次数列为条件，覆盖发送、降级、拒绝和幂等重试的结果。
--- explanation
先确认哪些消息必须送达、哪些可降级，以及用户关闭通知后的合规边界。代表列包括主渠道可用、主渠道失败但备用渠道可用、全部失败、偏好关闭和重复事件。检查消息 ID、发送次数、退避和日志是否一致，不把第三方服务超时直接当作业务发送成功；真实号码和地址应由合成夹具替代。
{% endflashcard %}

{% flashcard basic id:testing-basics-a08-concept-1 deck:"测试基础" priority:1 tags:"判定表,横杠" %}
--- question
判定表中的横杠代表什么？
--- answer
它表示该条件对当前规则的动作没有区分作用，但前提是其他取值不会改变结果、错误、状态或证据。
--- explanation
借阅规则中会员无效时，库存是否可借不影响“拒绝借阅”，可以写横杠；如果产品要求错误提示优先级取决于库存，则不能横杠。横杠不是缺少测试数据的借口，合并前要检查所有可观察输出和副作用，并在追踪中说明依据。
{% endflashcard %}

{% flashcard basic id:testing-basics-a08-concept-2 deck:"测试基础" priority:1 tags:"规则简化,覆盖" %}
--- question
什么时候可以合并判定表中的规则列？
--- answer
当条件取值不同却产生完全相同的动作、状态、错误和可观察证据，并且不影响后续流程时才可合并。
--- explanation
若两个组合都拒绝，但一个返回错误码 A、一个返回错误码 B，不能合并；若日志审计、计费或库存副作用不同，也不能合并。合并后应保留规则到代表用例的追踪，必要时用一个代表值覆盖等价类，并记录被省略的组合和理由。
{% endflashcard %}

{% flashcard basic id:testing-basics-a08-concept-3 deck:"测试基础" priority:1 tags:"因果图,判定表" %}
--- question
因果图和判定表应该怎样配合？
--- answer
因果图先表达条件、结果和约束，判定表再把可行逻辑组合展开为可执行规则和用例。
--- explanation
因果图适合发现自然语言中的与/或/非、互斥和屏蔽关系；判定表适合逐列检查输入组合、动作和覆盖。先画图再列所有组合，标记不可行列，最后为每个保留列安排代表数据和预期。两者都不能直接处理长时序或资源竞争，必要时转到状态和并发模型。
{% endflashcard %}

## 参考资料

{% linkgroup %}
{% link ISTQB CTFL v4.0.1, https://istqb.org/wp-content/uploads/2024/11/ISTQB_CTFL_Syllabus_v4.0.1.pdf, https://istqb.org/wp-content/uploads/2024/10/cropped-favicon-32x32.png %}
{% endlinkgroup %}
