---
title: 测试基础(十)功能测试场景训练
tags:
  - 测试基础
  - 系统学习
categories:
  - Learn Topic
  - 测试基础
description: 把方法组合用于陌生场景并表达思路，独立给出澄清问题、功能模型、代表用例和未覆盖风险。
cover: /img/learn-topic/testing-basics/course-cover.png
series: 测试基础
series_order: 10
published: true
abbrlink: c75ad281
date: 2026-05-23 00:00:00
---

{% course_series %}

{% note info flat %}
场景题不是背产品细节，而是展示从澄清到证据的思考链：先问规则和边界，再拆功能与状态，选择等价类、判定表或场景法，最后给代表用例、观察点和未覆盖风险。本篇的产品与数值都是教学假设，不代表真实厂商规则。
{% endnote %}

## 作答框架

### 澄清与假设

{% mermaid %}
flowchart TD
  Q[澄清目标与假设] --> F[功能树与状态]
  F --> M[选择测试方法]
  M --> C[代表用例与预期]
  C --> E[证据、遗漏与追问]
{% endmermaid %}

{% note primary flat %}
口头回答时先说明测试对象、用户、规则、数据和环境，再给成功、主要失败和恢复路径。每条用例都保留前置、数据、步骤、预期、优先级和清理；没有规则时提出问题并标记假设，不用“功能正常”收尾。
{% endnote %}

### 范围与观察

![测试思维从提问、建模到证据复盘的循环](/img/learn-topic/testing-basics/testing-thinking.jpeg "测试思维")

## 客服聊天

### 会话与消息

| 功能 | 代表检查 | 关键证据 |
| --- | --- | --- |
| 入口与排队 | 登录后进入客服、排队、取消 | 会话 ID、接入状态、退出重进 |
| 消息 | 文本、图片、长消息、重复点击 | clientMessageId、顺序、已发送/失败 |
| 恢复 | ACK 丢失、弱网、客服离线 | 重试幂等、历史一致、提示可行动 |
| 结束 | 结束会话、再次进入 | 历史边界、隐私和权限 |

{% note info flat %}
把“客服聊天页面”回答成用例时，可以先给出一组可执行代表值。假设访客登录后创建会话，消息带唯一 `clientMessageId`，服务端返回发送确认。
{% endnote %}

| 用例 | 前置与操作 | 功能预期 |
| --- | --- | --- |
| CHAT-01 | 登录用户进入客服；人工在线且队列为空 | 立即创建一个会话 ID，显示“人工接入”；刷新后仍进入同一会话 |
| CHAT-02 | 输入空文本、501 字符文本和含表情文本 `你好🙂` | 空文本不可发送；501 字符提示“最多 500 字符”；合法文本按原顺序展示并入历史 |
| CHAT-03 | 发送 500 KiB JPEG `proof.jpg`，随后切换后台再返回 | 上传状态可见，成功后可预览原图；失败可重试且不产生重复消息 |
| CHAT-04 | 首次请求 ACK 丢失，使用同一 `clientMessageId` 重试 | 最终只有一条服务端消息，客户端从发送中转为已发送或明确失败 |
| CHAT-05 | 客服离线、普通队列已满 2 人、用户主动取消 | 离线进入自助；满载显示排队已满并提供留言；取消后重进不会创建第二个会话 |
| CHAT-06 | 用户结束会话后再次进入 | 历史只读边界符合需求；新会话与旧会话 ID、消息和权限隔离 |

### 异常与一致性

{% note primary flat %}
CHAT-04 至 CHAT-06 应同时比较页面状态、接口回执、历史记录和会话 ID；只看气泡颜色不能证明服务端没有重复写入。
{% endnote %}

{% note warning flat %}
“已读、撤回、转人工”只有在题面确认后才纳入必测；未知能力应先追问。性能和隐私是补充风险，不要用它们替代聊天功能主线。
{% endnote %}

## 红包场景

### 发送与支付

| 阶段 | 功能路径 | 需检查的边界 |
| --- | --- | --- |
| 创建 | 选择个人/群、金额和份数 | 余额、金额精度、份数上限（假设） |
| 支付 | 虚拟支付成功/失败/超时 | 幂等查单、不可重复扣款 |
| 领取 | 成员资格、一次领取、领完 | 并发竞争、金额守恒 |
| 结束 | 过期、退回、查看记录 | 退回金额与原记录一致 |

{% note info flat %}
红包题的关键不是背平台上限，而是把账务和状态说清。下面假设虚拟账户各有 100.00 元，红包金额 10.00 元、份数 2、有效期 60 秒。
{% endnote %}

| 用例 | 前置与操作 | 功能预期 |
| --- | --- | --- |
| RP-01 | 群成员 A 创建 10.00/2 份红包，余额足够并支付成功 | 红包状态为可领取，余额扣 10.00，记录金额和份数一致 |
| RP-02 | 余额 9.99 创建同红包 | 创建或支付被拒绝，余额、红包记录和群消息均不产生半成品 |
| RP-03 | 成员 B 在有效期内领取一次，再次点击 | 首次产生一条领取记录并减少剩余；第二次明确拒绝且金额不变 |
| RP-04 | 非群成员尝试领取、份数领完后再领 | 两者均拒绝，不改变已领取金额或剩余份数 |
| RP-05 | 两个成员同时抢最后一份 | 只有一个成功，剩余为 0，领取记录数和金额守恒 |
| RP-06 | 创建后 59.999 秒和 60.000 秒分别领取，随后执行退回 | 59.999 秒可领取；60.000 秒拒绝；退回只发生一次，`已领取+剩余+已退回=10.00` |
| RP-07 | 支付响应超时后查询并重复提交 | 以订单/幂等键查到唯一支付结果，不重复扣款或创建红包 |

### 领取与退回

{% note primary flat %}
RP-03 至 RP-06 要把领取记录、剩余份数、用户余额和退回金额放在同一条预期中，才能发现页面与账务不一致。
{% endnote %}

## 实物场景

### 杯子用途

{% note primary flat %}
测试“杯子”时先把功能定义为盛装、放置、持握、倾倒和按需求密封；外观、材质安全和寿命是另列的质量属性。无盖杯、儿童杯和保温杯改变用途与风险，应删改用例，而不是把所有产品都套同一清单。
{% endnote %}

| 物品 | 主要功能模型 | 变化后要追问 |
| --- | --- | --- |
| 有盖常温水杯 | 装水→盖紧→携带→饮用 | 容量、密封、冷热用途 |
| 水瓶 | 灌装→旋盖→携带→饮用 | 标定容量、吸管、重复开合 |
| 雨伞 | 打开→锁定→遮雨→收拢 | 自动机构、尺寸、喷水条件 |
| 电梯面板（模拟） | 呼叫→开门→选层→到达 | 楼层边界、超载、门未关 |

{% note info flat %}
杯子题也要先声明可观察的功能。假设是有盖、标称 350 mL 的常温水杯，允许手动开合；测试使用量杯和可控倾斜装置，杯盖带红/绿机械锁定指示片，容量边界通过液面是否越过杯口观察，不依赖电子提示。
{% endnote %}

| 用例 | 前置与操作 | 功能预期 |
| --- | --- | --- |
| CUP-01 | 空杯放在水平桌面，连续放置 10 分钟 | 杯体稳定不自行倾倒，杯底与桌面接触正常 |
| CUP-02 | 加水 1 mL、350 mL、351 mL | 1 mL 和 350 mL 可盛装；351 mL 液面越过杯口并发生溢出，不把超量当作正常容量 |
| CUP-03 | 盖子未扣紧/扣紧后倒置 30 秒；机械锁定指示片可见 | 未扣紧显示红色且允许泄漏；扣紧显示绿色，倒置无可观察泄漏 |
| CUP-04 | 空杯、175 mL 和 350 mL 分别持握并倾倒 | 三种装量均可完成握持和倒出；液体从杯口流出且杯盖不自行弹开 |
| CUP-05 | 开合杯盖 100 次并重复放置 | 开合状态与初始一致；若出现卡滞、裂纹或密封变化，记录为功能失败并保留次数 |
| CUP-06 | 交换无盖、儿童杯或保温杯需求 | 重新确认容量、密封和温度规则；不把有盖常温杯的预期直接复制到新型号 |

### 功能边界

{% note warning flat %}
“能盛水”只是一个测试点，还要通过容量、开合、密封、携带和倾倒动作证明用户任务可以完成；材料安全、寿命和高温认证另立专项。
{% endnote %}

## 同类迁移

### 实物与设备

{% note info flat %}
从一个场景迁移到另一个场景时，迁移的是“澄清—建模—证据”方法，不是把用例文字复制过去。水瓶与杯子都涉及容量和密封，但吸管、旋盖和携带姿态会新增状态；交易类场景还要加入幂等、金额和补偿。
{% endnote %}

| 编号 | 场景题 | 主模型 | 与前篇的差异 |
| --- | --- | --- | --- |
| S26 | 水瓶 | 容量 × 旋盖 × 饮用口状态 | 新增旋盖螺纹和饮用口开闭 |
| S27 | 雨伞 | 关闭→打开→锁定→收拢 | 新增锁定守卫和喷水条件 |
| S28 | 模拟电梯面板 | 楼层 × 门状态 × 呼叫队列 | 新增边界楼层和门未关守卫 |

### 应用与交易

| 编号 | 场景题 | 主模型 | 与前篇的差异 |
| --- | --- | --- | --- |
| S29 | 模拟自动售货机 | 选货→支付→出货/退款 | 新增库存、支付与补偿守恒 |
| S30 | 注册到登录 | 账号状态 × 会话状态 | 新增验证码有效期和幂等注册 |
| S31 | 搜索旅程 | 输入 × 结果集 × 分页 | 新增旧响应与新查询的竞态 |
| S32 | 游客购物车 | 身份 × 数量 × 库存/价格 | 新增登录合并与账号隔离 |
| S33 | 下单支付 | 订单 × 支付 × 库存 | 新增取消/支付竞争和查单 |
| S34 | 售后退款 | 售后状态 × 可退余额 | 新增审核、部分退款和回调 |
| S35 | 文件上传 | 文件状态 × 分片 × 权限 | 新增摘要完整性和恢复偏移 |
| S36 | 预约购票 | 座位 × 预占 × 支付 | 新增预占超时和最后座位竞争 |

{% note info flat %}
S26–S36 是迁移题索引；面试时先说主模型，再挑一条成功路径、一条失败路径和一条恢复/竞争路径展开。每题的完整前置、步骤、预期、优先级和追问只在下方同名场景卡维护，避免索引与题面分叉。
{% endnote %}

## 追问与复盘

### 时间限制

- 时间限制：先交付哪三个 P1 用例，哪些风险明确延期？

### 规则变化

- 规则变化：哪条需求改变了输入域、状态或判定优先级？
- 外部依赖：超时、重复回调或结果未知时如何查证？
- 观察边界：页面、接口、数据、日志和用户任务分别能证明什么？

{% note success flat %}
优秀回答不在于列出最多名词，而在于让听者能复现你的假设、用例和结论。结束时主动说出未覆盖条件和需要业务决定的地方，体现风险意识而不是虚构穷尽。
{% endnote %}

## 常见问题

{% flashcard basic id:testing-basics-scenario-support-chat-full deck:"测试基础" priority:1 tags:"场景,客服聊天" %}
--- question
给定一个模拟 App 客服聊天页面，如何设计功能测试？
--- answer
先澄清入口、接入、消息、历史和结束规则，再以功能树、消息状态机和场景法覆盖发送、失败重试、切换与退出重进。
--- explanation
固定规则：教学页面要求登录后进入客服；人工在线且队列为空时直接接入，普通队列上限 2；人工离线进入自助；文本最多 500 字符，JPEG 图片不超过 1 MiB；同一 `clientMessageId` 幂等，结束会话只读。模型：会话状态 + 消息状态 + 入口/退出路径。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| CHAT-01 | 已登录；客服在线；空队列 | 打开客服→等待接入→观察会话 ID/状态 | 直接进入人工；只创建一个会话 ID，刷新后 ID 不变 | 1 | 入口基本流 |
| CHAT-02 | 会话已建立；文本 `订单还没到` | 输入→发送→观察发送中/成功、双方记录和顺序 | 一条消息；状态最终 sent；客服端可见相同内容 | 1 | 核心发送 |
| CHAT-03 | JPEG `proof.jpg`，500 KiB | 选择→发送→观察上传状态、预览、记录 | 图片可预览；失败重试沿用原 ID；客服端只见一条图片消息 | 1 | 类型/内容动作 |
| CHAT-04 | 服务端保存消息但 ACK 丢失；固定 `clientMessageId=m-1` | 发送→等待超时→按原 ID 重试→观察双方记录 | 只保留一条；顺序不变；状态从 sending 到 sent | 1 | 失败幂等 |
| CHAT-05 | 有历史消息；会话结束 | 结束→退出→重进→观察历史和状态 | 历史按规则保留；已结束会话不能误发新消息 | 1 | 生命周期 |
| CHAT-06 | 客服离线；网络正常；已登录用户 | 打开客服→发送文本→观察提示和出口 | 显示“人工离线”，进入自助；不创建人工会话 | 1 | 异常与分流 |

选择理由：入口、文本、图片、ACK 丢失、结束重进和离线覆盖必需状态边；每条都看界面与双方记录。遗漏与追问：新消息乱序、会话切换、重复点击、转人工/撤回/已读、隐私、性能和消息保留期限。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-wechat-redpacket-full deck:"测试基础" priority:1 tags:"场景,红包,守恒" %}
--- question
给定一个模拟发红包功能，如何设计功能测试？
--- answer
先固定个人/群、金额、份数和虚拟支付规则，再覆盖创建、支付、领取、领完、过期退回及并发守恒。
--- explanation
固定规则：只测试群红包；群内 3 人，创建者和成员账户各有 100.00 元；红包 10.00 元分 2 份，有效期 60 秒；非成员不可领取，过期剩余一次退回创建者。模型：创建→支付→可领取→领完/过期→退回，金额按分守恒。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| RP-01 | 群内 3 人；金额 10.00/2 份；支付成功 | 创建→支付→观察状态和余额 | 创建成功；冻结金额与红包总额一致；状态可领取 | 1 | 基本流 |
| RP-02 | 非成员 U；红包可领取 | U 打开→尝试领取→观察提示和记录 | 拒绝；不增加领取记录；余额不变 | 1 | 资格分区 |
| RP-03 | 成员 A 已领取一份 | A 再领→观察状态、记录和余额 | 拒绝/幂等；不重复增加余额 | 1 | 一次性约束 |
| RP-04 | 只剩最后一份；成员 A/B 同时请求 | 交错放行→观察成功人数、记录和剩余 | 仅一人成功；状态领完；金额守恒 | 1 | 并发边界 |
| RP-05 | 创建后 59.999 秒和 60.000 秒分别领取 | 固定时钟→提交领取→触发过期退回→观察状态/余额 | 59.999 秒可领取；60.000 秒拒绝；退回只发生一次 | 1 | 时间状态 |
| RP-06 | 支付请求超时，扣款结果未知；订单键 K1 | 查询 K1→按查询结果重试/取消→观察支付、红包和退款记录 | K1 只有一个支付终态；不重复扣款；补偿记录与 10.00 元一致 | 1 | 结果未知 |

选择理由：创建、资格、重复、最后一份、过期和支付未知覆盖全生命周期及守恒。遗漏与追问：普通/随机算法、群成员变化、网络重试、过期同时领取、退款失败和金额精度；任何真实上限需产品确认。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-cup-full deck:"测试基础" priority:1 tags:"场景,实物,功能拆解" %}
--- question
给定一个杯子，如何设计功能测试？
--- answer
先声明教学假设（有盖、常温水、容量范围），再按盛装、放置、持握、倾倒、开合和密封设计可观察用例。
--- explanation
固定规则：选择有盖、可重复使用、标称 350 mL 的常温水杯；盖子带红/绿机械锁定指示片，扣紧后倒置 30 秒不得漏水，未扣紧时指示片必须为红色；351 mL 以液面越过杯口判定溢出，不做高温或危险破坏。模型：用途/功能树 + 容量边界 + 开合状态。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| CUP-01 | 空杯；盖打开；装水 1 mL | 装水→观察容量和外观 | 可正常盛装 1 mL；杯体和桌面无渗漏 | 1 | 基本用途 |
| CUP-02 | 盖打开；装水 350 mL | 加水到 350 mL→观察刻度、液面和记录 | 达到 350 mL 不溢出；容量读数为 350 mL | 1 | 容量有效边界 |
| CUP-03 | 盖打开；已有 350 mL | 再加 1 mL→观察液面和状态 | 液面越过杯口并溢出；不把超量当作正常容量 | 1 | 上界外 |
| CUP-04 | 装水 350 mL；盖正确锁紧且指示片绿色 | 盖合→倒置 30 秒→观察泄漏、锁状态 | 无可观察泄漏；机械指示片保持绿色 | 1 | 开合/密封 |
| CUP-05 | 空杯；杯柄可用 | 持握、放置、倾倒→观察稳定、握持和液体方向 | 能安全完成操作；放置不自行倾倒 | 1 | 操作链路 |
| CUP-06 | 已开合 10 次 | 重复开合→观察铰链/螺纹状态和密封 | 次数内功能保持；若损坏属于寿命专项证据 | 1 | 重复状态 |

选择理由：盛装、容量、超量、密封、持握/放置和重复开合覆盖功能主线。遗漏与追问：无盖/儿童杯/保温杯需删改哪些功能，高温材质安全、清洁、跌落、寿命和认证应转专项，不用功能通过替代。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-bottle-transfer deck:"测试基础" priority:1 tags:"场景,水瓶,迁移" %}
--- question
如何把杯子的测试思路迁移到水瓶？
--- answer
保留用途拆解和容量边界，重新建模旋盖、饮用口、携带密封和重复开合等水瓶特有功能。
--- explanation
固定规则：水瓶带旋盖和标定容量 500 mL；装常温水，盖紧后倒置 30 秒不得漏水，饮用口打开才能出水。模型：灌装/饮用用途 + 旋盖状态 + 容量边界。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| BT-01 | 空瓶；盖打开 | 装入 500 mL→盖紧→观察刻度和状态 | 容量读数 500 mL；盖状态锁定 | 1 | 灌装基本流 |
| BT-02 | 装 500 mL 常温水；盖紧 | 倒置 30 秒并携带→观察泄漏、盖和内容物 | 无可观察泄漏；盖仍锁定 | 1 | 携带功能 |
| BT-03 | 盖打开；饮用口关闭/打开各一次 | 分别尝试饮用→观察流量和旋盖状态 | 饮用口关闭时不出水，打开时可连续出水；旋盖状态不变 | 1 | 饮用用途 |
| BT-04 | 已开合 10 次 | 重复旋盖→观察螺纹、密封和容量 | 次数内功能保持；异常转寿命专项 | 1 | 状态重复 |

选择理由：保留杯子的盛装/容量/密封思路，替换为水瓶特有旋盖和饮用口。遗漏与追问：增加吸管后的清洁、回流和密封，冷热用途、儿童误开、跌落与寿命。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-umbrella-transfer deck:"测试基础" priority:1 tags:"场景,雨伞,状态" %}
--- question
如何设计雨伞的功能测试用例？
--- answer
按关闭、打开、锁定、遮雨和收拢状态建模，在安全的喷水模拟条件下覆盖边界和非法操作。
--- explanation
固定规则：手动开合，具备锁定、收拢和遮雨用途；半开状态不能携带，锁定后在 10 mm/min 模拟雨量下遮挡有效；喷水是安全模拟，不执行真实危险天气试验。模型：关闭→打开→锁定→收拢的状态机。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| UT-01 | 雨伞关闭 | 推动打开→观察伞骨、锁定和遮挡 | 完全打开并锁定；状态可观察 | 1 | 基本转换 |
| UT-02 | 半开且未锁定 | 尝试携带→观察提示和状态 | 拒绝携带并提示“请先完全打开”；状态仍为半开 | 1 | 非法状态 |
| UT-03 | 已锁定；模拟雨量 10 mm/min、垂直喷水 60 秒 | 喷水→观察伞下是否进水和遮挡范围 | 伞下中心区域无持续滴水；状态仍为锁定 | 1 | 用途验证 |
| UT-04 | 已打开 | 按收拢→再打开→观察锁定和重复次数 | 收拢后状态关闭；再次打开可恢复；不产生卡滞 | 1 | 循环边 |

选择理由：开合、锁定、遮雨和收纳覆盖功能状态；喷水量/角度属于明确假设。遗漏与追问：折叠伞/自动伞的按钮、弹簧夹伤、强风、耐久和材料安全需新需求及专项方案。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-elevator-sim deck:"测试基础" priority:1 tags:"场景,状态机,电梯" %}
--- question
如何测试一个模拟电梯控制面板？
--- answer
用状态机覆盖内外呼叫、方向、楼层边界、开关门和门未关禁止运行，并明确仿真不等于安全认证。
--- explanation
固定规则：仅测试 1～10 层仿真控制面板；门打开时禁止运行，超载信号为 true 时禁止关门；队列按距离相同则先到先服务；不以此证明真实设备安全认证。模型：楼层/门状态/呼叫队列状态机。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| EL-01 | 电梯在 1 层、门关闭 | 发 10 层外呼→进入→观察楼层和门状态 | 接收呼叫；依次到达 10 层后开门；不越界 | 1 | 楼层边界 |
| EL-02 | 电梯在 10 层、门关闭 | 发 1 层内呼→观察方向、楼层和到达 | 向下运行；到达 1 层后开门；不越界 | 1 | 另一边界 |
| EL-03 | 门打开或超载信号 true | 按运行→观察按钮反馈和错误 | 禁止运行并提示对应原因；楼层保持不变 | 1 | 守卫条件 |
| EL-04 | 已有两个呼叫；取消其中一项 | 依次呼叫/取消→观察队列顺序和记录 | 队列按规则更新；取消不误服务已取消楼层 | 1 | 队列状态 |

选择理由：首层/顶层、内外呼、门守卫和取消覆盖仿真功能主线。遗漏与追问：多层同时呼叫、断电恢复、传感器故障、真实安全认证和物理夹伤；后者不得用仿真结论替代。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-vending-sim deck:"测试基础" priority:1 tags:"场景,交易,自动售货机" %}
--- question
如何测试一个模拟自动售货机？
--- answer
按选货、库存、支付、出货和退款状态建模，覆盖支付不足、缺货、支付成功未出货和重复点击。
--- explanation
固定规则：商品 A 售价 5.00 元、库存 1；虚拟账户余额可查询；支付成功后出货，支付成功但出货失败时自动全额退款。模型：选货→待支付→已支付→出货/补偿，库存和余额是可观察不变量。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| VM-01 | 商品 A 库存 1、价格 5；余额足够 | 选 A→支付→观察出货、订单和库存 | 出货一次；库存减 1；支付与订单匹配 | 1 | 基本交易 |
| VM-02 | 商品 A 库存 0 | 选 A→观察提示→尝试支付 | 提示缺货；不扣款、不出货、不建订单 | 1 | 库存无效类 |
| VM-03 | 商品 A 库存 1；余额 4.99 | 选 A→支付 4.99→观察余额和状态 | 拒绝余额不足；余额仍 4.99；库存仍 1 | 1 | 支付边界 |
| VM-04 | 商品 A 库存 1；支付 5.00 成功；出货故障 | 支付→制造出货失败→查询补偿→观察记录 | 自动退款 5.00；库存仍 1；不重复扣款或出货 | 1 | 失败补偿 |
| VM-05 | 商品 A 库存 1；两个请求同时选货 | 交错支付→观察库存、订单和出货 | 只有一个订单出货；库存变 0；另一请求明确缺货 | 1 | 竞争边界 |

选择理由：缺货、支付不足、成功、出货失败和最后一件竞争覆盖交易状态及守恒。遗漏与追问：找零、商品过期、支付超时、重复点击、优惠和真实支付合规；本题使用虚拟支付。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-account-journey deck:"测试基础" priority:1 tags:"场景,注册登录,流程" %}
--- question
如何测试注册到登录的完整流程？
--- answer
沿注册、验证、登录、退出和受保护页面状态验证关系约束，并覆盖已存在账号、验证中断和结果未知重试。
--- explanation
固定规则：流程为注册→验证→登录→退出；验证码为 6 位数字且 5 分钟有效；账号邮箱唯一，注册成功响应丢失时用幂等键查询；退出后会话立即失效。模型：账号状态 + 会话状态 + 结果未知重试。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| AC-01 | 新邮箱 `a@example.test`；验证码 `123456` 未过期 | 注册→验证→登录→观察账号、会话和受保护页 | 账号唯一创建；验证成功；会话主体为 A | 1 | 基本旅程 |
| AC-02 | 已存在账号 `a@example.test` | 用同邮箱重复注册→观察提示、账号记录 | 返回“邮箱已注册”；不创建第二账号 | 1 | 唯一性 |
| AC-03 | 验证码 `123456` 已过期 5 分钟 | 注册→输入过期验证码→重发→观察状态 | 账号保持待验证；旧码拒绝，新码可验证 | 1 | 中断状态 |
| AC-04 | 注册成功响应丢失；幂等键 K1 | 提交→丢响应→查询 K1→再次提交 | K1 只创建一个账号；查询与重试返回同一结果 | 1 | 结果未知 |
| AC-05 | 已登录后退出 | 退出→访问受保护页→观察会话 | 会话失效；受保护入口要求重新登录 | 1 | 终态 |

选择理由：新建、重复、验证中断、响应丢失和退出覆盖跨页面状态。遗漏与追问：密码找回、设备并行登录、会话过期、账号锁定、第三方验证和隐私提示。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-search-journey deck:"测试基础" priority:1 tags:"场景,搜索,结果集" %}
--- question
如何测试一个完整搜索功能？
--- answer
固定商品夹具和分页排序规则，覆盖输入、清空、筛选、分页、返回、无结果及结果集合稳定性。
--- explanation
固定规则：夹具包含 A、B、C 三件商品；每页 2 条，按商品 ID 升序，筛选条件保留在 URL；清空搜索恢复全部 3 条，不测试真实排名算法。模型：输入状态 + 结果集合 + 分页状态。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| SJ-01 | 关键词命中商品 A | 输入 `A`→提交→观察 ID 集合、计数、页码 | 只返回 A；计数 1；页码 1 | 1 | 单命中 |
| SJ-02 | 关键词命中 A、B、C | 搜索→下一页→返回→观察集合和页码 | 第 1 页 A/B，第 2 页 C；返回保留第 2 页 | 1 | 分页 |
| SJ-03 | 关键词 `Z` 无命中 | 搜索→观察空状态和请求 | 显示无结果；计数 0；页码 1 | 1 | 无效结果 |
| SJ-04 | 关键词 `A` 且筛选“可售” | 清空关键词和筛选→提交→观察结果集合和计数 | 恢复 A/B/C 全集；URL 无筛选参数 | 1 | 状态重置 |
| SJ-05 | 快速修改关键词或重复提交 | 连续输入/提交→观察最终请求和结果 | 旧响应不覆盖新结果；结果顺序稳定 | 1 | 竞态边界 |

选择理由：单命中、多页、无结果、清空和快速修改覆盖功能状态。遗漏与追问：大小写/Unicode、空查询、超长输入、联想、排序 tie、网络重试和真实排名规则。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-cart-journey deck:"测试基础" priority:1 tags:"场景,购物车,一致性" %}
--- question
如何测试购物车从游客到结算的完整流程？
--- answer
先确认游客持久化和登录合并规则，再覆盖勾选、库存/价格变化、重进和结算前后的数据一致性。
--- explanation
固定规则：游客购物车保存在会话；登录时同 SKU 数量相加但最多 5 件；库存不足时结算阻止；价格变化提示用户确认；只结算勾选项。模型：游客/登录身份状态 + 商品数量/价格状态。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| CJ-01 | 游客；商品 A 库存 5；购物车为空 | 加购 2 件→刷新→观察数量和价格 | 会话保留 2 件 A；无重复行 | 1 | 游客持久化 |
| CJ-02 | 游客有 A×2；账号已有 A×4 | 加购→登录→观察合并结果和数量 | 合并为 A×5（上限 5）；显示一次提示；不丢数据 | 1 | 跨身份关系 |
| CJ-03 | 购物车 A×2；库存降为 1；单价从 10 改 12 | 刷新→进入结算→观察提示、金额和可选状态 | 提示库存/价格变化；阻止提交，服务端不创建订单 | 1 | 外部变化 |
| CJ-04 | 多商品；取消勾选一项 | 勾选/取消→进入结算→观察选中集合和金额 | 只结算勾选项；购物车总数和小计可解释 | 1 | 选择状态 |
| CJ-05 | 同账号跨页面重进；再换另一账号 | 重进/退出登录→观察购物车和权限 | 数据按账号隔离；未授权用户不可见 | 1 | 生命周期与权限 |

选择理由：游客、合并、库存/价格变化、勾选和账号隔离覆盖跨页面功能。遗漏与追问：多设备同时修改、优惠券、删除最后一项、离线编辑、合并冲突责任和结算失败恢复。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-checkout-journey deck:"测试基础" priority:1 tags:"场景,下单,幂等" %}
--- question
如何测试下单支付流程？
--- answer
以合成商品、地址和虚拟支付为夹具，覆盖创建、重复提交、取消、支付失败/超时查单及订单支付记录一致性。
--- explanation
固定规则：商品 A 价格 100.00、库存 1；地址 `ADDR-1` 有效；支付为虚拟服务；取消先到则支付拒绝，支付先到则取消生成全额退款。模型：创建订单→待支付→已支付/已取消，库存与支付记录守恒。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| CO-01 | 商品库存 1、地址有效；幂等键 K1 | 提交订单→支付→观察订单、库存、支付记录 | 只创建一单；扣库存一次；支付与订单匹配 | 1 | 成功路径 |
| CO-02 | 同一购物车/键 K2 | 连续点击提交→观察订单数和响应 | 返回同一订单；不重复扣库存/支付 | 1 | 幂等 |
| CO-03 | 支付结果未知；订单键 K3 | 查询 K3→按查询结果重试→观察补偿 | K3 只有一个支付终态；不重复扣 100.00 | 1 | 结果未知 |
| CO-04 | 订单 O4；取消请求先到版本 1 | 先取消→再支付→观察订单、支付和库存 | 订单已取消；支付拒绝；库存仍为 1 | 1 | 竞争状态 |
| CO-05 | 结算时库存 1，提交前降为 0 | 结算→将库存改为 0→提交→观察错误、金额和库存 | 返回库存不足；不创建订单、不扣款、库存为 0 | 1 | 判定条件变化 |

选择理由：成功、重复、超时、竞争和提交时变化覆盖下单状态。遗漏与追问：地址变更、拆单、优惠叠加、支付回调乱序、退款失败和真实支付合规。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-after-sale-journey deck:"测试基础" priority:1 tags:"场景,售后,退款" %}
--- question
如何测试退款售后完整流程？
--- answer
沿申请、审核、退货、退款状态覆盖拒绝后修改、重复请求、部分退款和金额记录一致性。
--- explanation
固定规则：流程为申请→审核→退货→退款；普通商品签收后 7 个自然日内可退，特殊类别一律不可退；订单与原支付关联，部分退款不得超过可退余额。模型：售后状态 + 金额状态。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| AS-01 | 普通商品 100.00；签收后 3 天；可退余额 100.00 | 申请→审核→退货→退款→观察订单、余额和原支付 | 状态依次转换；退款 100.00；原支付与退款单匹配 | 1 | 基本旅程 |
| AS-02 | 普通商品；申请缺少凭证 | 提交→观察拒绝→补充凭证 `proof.jpg`→再次提交 | 首次拒绝“缺少凭证”；补充后进入审核；不创建重复售后单 | 1 | 拒绝后修改 |
| AS-03 | 可退余额 60；申请 60/61 | 分别提交→观察退款单、余额和订单 | 60 成功；61 拒绝；余额不为负 | 1 | 金额边界 |
| AS-04 | 同一退款请求重复提交 | 连续提交→观察记录、余额和回调 | 只生成一条退款单；结果幂等 | 1 | 重复请求 |
| AS-05 | 订单含 A 100.00、B 50.00；仅 A 进入售后 | 退 A→观察整单与明细状态 | A 售后/退款 100.00；B 仍为已完成；整单总额 150.00 可重算 | 1 | 组合影响 |

选择理由：成功、拒绝补充、金额边界、幂等和部分售后覆盖状态与关联记录。遗漏与追问：审核权限、退货物流、支付回调乱序、超时补偿、整单取消和真实平台/法律规则。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-upload-journey deck:"测试基础" priority:1 tags:"场景,上传,权限" %}
--- question
如何测试文件上传的完整流程？
--- answer
从输入约束到上传、预览、下载、删除和恢复建立生命周期用例，并核对内容完整性、权限与临时资源。
--- explanation
固定规则：仅允许 PNG/JPEG，单文件不超过 10 MiB；成功文件可预览/下载/删除；断线从最后确认分片恢复；重复提交生成新版本；文件只属于上传用户。模型：选择→上传中→成功/失败→预览/下载/删除生命周期。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| UJ-01 | 用户 A；1 MiB PNG 文件 F，摘要 H | 选择→上传→预览→下载→比对字节摘要和文件 ID | 上传成功；预览/下载摘要均为 H；记录归属 A | 1 | 基本生命周期 |
| UJ-02 | 用户 A；11 MiB PNG 文件 F | 选择→上传→观察错误、临时分片和列表 | 拒绝“超过 10 MiB”；无可见半成品；列表无 F | 1 | 输入失败 |
| UJ-03 | 上传中断/断网 | 上传→断网→恢复→重试→观察偏移和摘要 | 从正确位置恢复；不重复写入；最终摘要正确 | 1 | 恢复 |
| UJ-04 | 用户 A；已成功文件 F；重复提交 | 重复上传→删除版本 2→访问旧链接→观察记录和权限 | 生成版本 2；删除版本 2 后不可访问；版本 1 仍可访问；临时资源清理 | 1 | 终态与清理 |
| UJ-05 | 用户 B 访问 A 的文件 | 切换用户→打开列表/链接→观察响应和审计 | B 不可见/不可下载；权限错误不泄露内容 | 1 | 归属边界 |

选择理由：合法内容、输入错误、恢复、终态清理和跨用户权限覆盖生命周期。遗漏与追问：多文件部分成功、刷新继续、同名覆盖、病毒扫描、预览转码和存储故障。
{% endflashcard %}

{% flashcard basic id:testing-basics-scenario-ticket-booking deck:"测试基础" priority:1 tags:"场景,预约购票,竞争" %}
--- question
如何测试预约购票流程？
--- answer
以虚拟场次和座位为夹具，沿可用→预占→支付→出票覆盖超时释放、重复操作和最后座位竞争。
--- explanation
固定规则：虚拟场次 S1 的座位 A1/A2，预占有效期 10 分钟；支付成功出票，支付未知先查单；取消订单释放座位且不可再次支付；不调用真实售票服务。模型：可用→预占→支付→出票/释放。

| 用例 | 前置与数据 | 步骤与观察 | 预期 | 优先级 | 选择理由 |
| --- | --- | --- | --- | ---: | --- |
| TB-01 | 场次 S1；座位 A1 可用；支付 30.00 成功 | 选座→预占→支付→观察座位、订单、出票 | 预占成功；出票一次；A1 终态已售；订单金额 30.00 | 1 | 基本旅程 |
| TB-02 | A1 预占已满 10 分钟且未支付 | 固定时钟前进→再次选 A1→观察释放和记录 | A1 释放为可用；旧订单不可支付；释放只记录一次 | 1 | 时间边界 |
| TB-03 | 两用户竞争最后座位 A2 | 同时预占→观察版本/顺序和订单 | 仅一人预占成功；另一人返回座位已占用；无重复出票 | 1 | 竞争边界 |
| TB-04 | 支付结果未知；订单键 K4 | 查询 K4→按查询结果重试/取消→观察座位和退款 | K4 只有一个支付终态；座位/退款与终态一致；不重复扣 30.00 | 1 | 结果未知 |
| TB-05 | 已取消订单 O5；座位 A1 已释放 | 尝试再次支付→观察状态和记录 | 返回“订单已取消”；不扣款、不出票、不重新占用 A1 | 1 | 状态转换 |

选择理由：成功、超时释放、竞争、支付未知和取消后非法操作覆盖预约状态。遗漏与追问：改场次、退款、通知失败、座位图延迟、批量选座和真实服务合规。
{% endflashcard %}

## 参考资料

{% linkgroup %}
{% link ISTQB CTFL v4.0.1, https://istqb.org/wp-content/uploads/2024/11/ISTQB_CTFL_Syllabus_v4.0.1.pdf, https://istqb.org/wp-content/uploads/2024/10/cropped-favicon-32x32.png %}
{% endlinkgroup %}
